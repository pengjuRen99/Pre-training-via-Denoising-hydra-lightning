# @package _global_

defaults:
  - _self_
  - data: pcqm4mv2 # choose datamodule with `test_dataloader()` for evaluation
  - model: pcqm4mv2
  - logger: null
  - trainer: gpu
  - paths: default
  - extras: default
  - hydra: default

task_name: "eval"

tags: ["dev"]

# passing checkpoint path is necessary for evaluation
ckpt_path: ???
